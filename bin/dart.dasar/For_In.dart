void main(List<String> args) {
  

  
  var names = <String>['Hijrina', 'Halisa',];

  for (var value in names) {
    print(value);
  }

  //Gunakan set
  var nameSet = <String>{'Hijrina', 'Halisa',};
  for (var value in nameSet) {
    print(value);
  }
}